#pragma once

// Headerek felosztása
/* engine.hpp - EZ -> Összefoglaló a motorról
 * context.hpp -> az intelligens rész, a Context struct
 * + state.hpp -> A State machine state, csak adat.
 * common.hpp -> enum, ilyenek.
 * states/ -> összes rendszer saját mappájával.
 * ( states/renderer pld)
 * /renderer.hpp -> összefoglaló
 * /handle.hpp -> Handle és API
 * /state.hpp -> Csak egy struct adattagokkal és schema-val
 * /schema.hpp -> Beállítások, ugye a schema.
 * */

#include "context.hpp"
