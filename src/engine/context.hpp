#pragma once 

#include <cstdint>

namespace engine {
  
  // függvények nem tudom minek, tick idő, delta time, stb.
  inline uint64_t ticks = 1; 
  inline bool should_shutdown = false;
  inline float delta_time = 0;
}
